title: easy1090
description: "One command installer for a complete ADS-B receiver on Arch Linux: RTL-SDR driver, readsb and the tar1090 web map."

# The default Pages gem set does not ship the theme gem; remote_theme is
# resolved natively by the github-pages gem. Pinned for determinism.
remote_theme: mmistakes/minimal-mistakes@4.28.1
minimal_mistakes_skin: dark

# minimal-mistakes uses include_cached, provided by this whitelisted plugin
plugins:
  - jekyll-include-cache
  - jekyll-remote-theme

url: "https://esli.cafe"
baseurl: "/easy1090"

search: true
search_provider: lunr
breadcrumbs: true

# Header navigation lives in _data/navigation.yml
